<!DOCTYPE html>
<html>
	<head>
		<title>Practicing with Sinatra and Angular</title>
		<script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.28/angular.min.js"></script>
		<script src="js/controllers/test_controller.js"></script>
		<link href="lib/bootstrap/css/bootstrap.min.css" rel="stylesheet">
	</head>
	<body>
		<div class="container" ng-app="testApp">
			<h1>Welcome to the Number Cruncher</h1>
			<p>Enter a number and I will churn out some useful information for you</p>
			<div ng-controller="TestController as testController">
				<form >
					<div class="form-group">
						<label for="number-input">Enter number</label>
						<input type="text" class="form-control" id="number-input" ng-model="number">
					</div>
					<button class="btn btn-primary" ng-click="testController.submitNumber(number)">Submit</button>
				</form>
				<br>
				<div class="well" ng-show="numberSubmitted">
<<<<<<< HEAD:main/web/views/index.erb
					<h3>Results</h3>
					<p><strong>Number: </strong>{{ results.number }}</p>
					<p><strong>Factors: </strong><span ng-repeat="factor in results.factors">{{ factor }} </span></p>
					<p><strong>Prime number? </strong>{{ results.prime }}</p>
=======
					<h4>Results for {{ numberData.number }}</h4>
					<p><strong>Factors: </strong><span ng-repeat="factor in numberData.factors">{{ factor }} </span></p>
					<p><strong>Prime number? </strong>{{ numberData.prime.toString() | uppercase }}</p>
					<p><strong>Even? </strong>{{ numberData.even.toString() | uppercase }}</p>
>>>>>>> commit6747043:web/views/index.erb
				</div>
			</div>
		</div>
	</body>
</html>